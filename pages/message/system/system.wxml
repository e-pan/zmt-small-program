<view class='message-detail'>
	<ul>
		<li wx:for='{{ lists }}' wx:key='key'>
			<view class='tit'>
				<text>{{ item.title }}</text>
				<text class='date'>{{ item.messageDate }}</text>
			</view>
			<view class='con'>{{ item.context }}</view>
		</li>
	</ul>
</view>